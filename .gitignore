# ── Python ────────────────────────────────────────────────────────────
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
.pytest_cache/
.coverage
htmlcov/

# ── Frontend ──────────────────────────────────────────────────────────
node_modules/
# The dashboard is built by Docker (or `npm run build` locally); there is no
# reason to keep the output in the repository.
dist/
*.tsbuildinfo
.vite/

# ── Data ──────────────────────────────────────────────────────────────
# The database and its WAL files: everyone has their own, demo ones included.
*.db
*.db-shm
*.db-wal

# ── Environment ───────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── Scratch ───────────────────────────────────────────────────────────
# A throwaway repository the demo agents build into. It is a test of noted,
# not part of it.
demo_agent_project/
# Local MCP wiring for the demo: it carries real agent keys.
.mcp.json

# ── Editors and OS ────────────────────────────────────────────────────
.DS_Store
.idea/
.vscode/
*.swp
