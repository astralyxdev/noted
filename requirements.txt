fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
mcp>=2.0,<3
pydantic>=2.7
python-dotenv>=1.0
# Only used when NOTED_DB_URL is set. In the image it is always present, so
# moving to PostgreSQL is one environment variable and no rebuild.
psycopg[binary,pool]>=3.2
